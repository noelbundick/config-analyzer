import 'mocha';
import {expect} from 'chai';

describe('First dummy test', () => {
  it('Confirms that mocha/chai/ts-node set up is working properly', () => {
    expect(true).to.equal(true);
  });
});
